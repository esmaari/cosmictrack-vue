# Vite / Vue / Node.js
node_modules/
dist/

# Env — local secrets (Vite loads .env.local over .env). .env.example stays tracked.
.env
.env.local
.env.*.local

# TypeScript incremental build cache
*.tsbuildinfo

# Supabase CLI local cache (project ref, versions — not for version control)
supabase/.temp/

# Logs / debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
